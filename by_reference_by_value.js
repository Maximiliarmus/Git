// Создай две переменные, скопируй значение одной в другую. Затем измени вторую переменную и проверь, изменится ли первая.

let num1 = 100;
let num2 = num1;

num2 = 200;

console.log(num1);
console.log(num2);

// By Value = создается новая копия данных, изменения не влияют на оригинальную переменную.

// Создай объект, передай его в другую переменную и измени его свойства. Проверь, изменится ли оригинальный объект.

let obj = { name: "Алиса" };
let obj2 = obj;
obj.name = "Игорь"

console.log(obj);
console.log(obj2);

// В отличие от примитивных типов, объекты передаются по ссылке (By Reference).
// Когда мы пишем let user2 = user1;, мы копируем не сам объект, а ссылку на него.

// 🔹 То есть, user1 и user2 ссылаются на одну и ту же область памяти, а не на разные объекты.
// 🔹 Когда мы изменяем user2.name, мы изменяем сам объект, и user1.name тоже обновляется.

// Создай массив, передай его в другую переменную и измени один из элементов. Проверь, изменится ли оригинальный массив.

let arr1 = [1, 2, 3];
let arr2 = arr1;

arr2[2] = 4;

console.log(arr1);
console.log(arr2);

// Массивы (как и объекты) передаются по ссылке (By Reference).
// Когда мы пишем let arr2 = arr1;, мы не создаем новый массив, а просто копируем ссылку на уже существующий массив в памяти.

// 🔹 То есть, arr1 и arr2 теперь указывают на один и тот же массив.
// 🔹 Когда мы меняем arr2[0], на самом деле мы изменяем сам массив в памяти, и arr1[0] тоже меняется.

// Используй оператор spread (...) для создания копии объекта. Проверь, будет ли изменяться оригинал.

let user1 = { name: "Олег", age: 25, hobby: "basketball" }
let user2 = { ...user1 };
user2.hobby = "football";
user2.age = 26;

console.log(user1);
console.log(user2);

// Так как spread-оператор {...user1} создает новый объект, свойства name, age и hobby копируются по значению (By Value).
// user1 и user2 хранят разные объекты в памяти, поэтому изменение свойств user2 не влияет на user1.

// Используй JSON.parse(JSON.stringify(...)) для создания полной копии объекта. Проверь, что изменения не затронут оригинал.

let obj3 = { name: "Max", details: { age: 30, hobby1: "resling" } }
let obj4 = JSON.parse(JSON.stringify(obj3));

obj4.name = "Maxim";
obj4.details.hobby1 = "boxing";

console.log(obj3);
console.log(obj4);

// obj4 — это глубокая копия obj3, поэтому изменения в obj4 не затрагивают obj3.

// Функции в JavaScript передаются по ссылке. Проверь, могут ли две переменные ссылаться на одну и ту же функцию.

let greet = function (name1) {
    console.log(`Привет, ${name1}`);
}
greet("Макс");

let greetCopy = greet;

greetCopy("Олег");

// Аргумент функции не меняется, потому что:

// Для примитивных типов данных (например, строк, чисел и булевых значений) передача аргумента происходит по значению.
// Любые изменения в параметре функции влияют только на локальную копию этого значения, а не на оригинальную переменную.

// Передай объект в функцию и измени его внутри. Проверь, изменится ли оригинальный объект.

function updateUser(user) {
    user.age = 30;
}
let person = { name: "Anna", age: 25}

updateUser(person);

console.log(person.age);

// После того как функция выполнилась, ты выводишь значение age объекта person, и оно уже равно 30, потому что изменения были внесены напрямую в тот же объект, на который указывают обе переменные (person и user).
