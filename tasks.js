// Что выведет код?
console.log(0.1 + 0.2 === 0.3);
// На первый взгляд кажется, что 0.1 + 0.2 должно быть 0.3, но это не так.
// 🔹 Дело в том, что в JavaScript (и во многих других языках) числа с плавающей точкой хранятся в формате IEEE 754, который иногда не может точно представить десятичные дроби.
// Из-за этого сравнение 0.1 + 0.2 === 0.3 возвращает false.

console.log(a);
var a = 5;

// 💡 Ответ: undefined (из-за hoisting).

// 3. Разница между == и ===?
console.log(0 == "0");  // ?
console.log(0 === "0"); // ?

// 💡 Ответ:

// 0 == "0" → true (неявное приведение типов)
// 0 === "0" → false (разные типы)

// 📌 4. Найти сумму всех чисел в массиве

const numbers = [1, 2, 3, 4, 5];
const sum = numbers.reduce((acc, num) => acc + num, 0)
console.log(sum);

// Как работает .reduce()?
// Метод .reduce() используется для сворачивания массива в одно значение (например, сумму, произведение, объединение строк и т. д.).
// array.reduce((accumulator, currentValue) => { ... }, initialValue);
// accumulator (аккумулятор) — переменная, в которой накапливается результат.
// currentValue (текущий элемент) — текущий обрабатываемый элемент массива.
// initialValue — начальное значение аккумулятора.

// 5. Удалить дубликаты из массива

const arr = [1, 1, 2, 2, 3, 4, 4, 5, 5];
const newArr = [...new Set(arr)]
console.log(newArr)

// new Set(arr)
// Set — это специальный объект в JavaScript, который хранит только уникальные значения.
// Когда мы передаём в него массив arr, он автоматически удаляет дубликаты.



